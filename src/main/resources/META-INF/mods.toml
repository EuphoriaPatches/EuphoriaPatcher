modLoader="javafml" #mandatory
loaderVersion="${loader_version_range}"
issueTrackerURL="https://discord.gg/5N45SAsC3X"
license="${mod_license}"
# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]] #mandatory
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
# A URL to query for updates for this mod. See the JSON update specification <here>
#updateJSONURL="http://myurl.me/" #optional
displayURL="https://discord.gg/5N45SAsC3X" #optional
logoFile="logo.png" #optional
#credits="Thanks for this example mod goes to Java" #optional
authors="${mod_authors}" #optional
description='''${mod_description}'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]] #optional
modId="forge"
mandatory=true
# The version range of the dependency
versionRange="${forge_version_range}"
ordering="NONE"
side="BOTH"
[[dependencies.${mod_id}]]
modId="minecraft"
mandatory=true
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"

# Optional Dependencies
[[dependencies.${mod_id}]]
modId="oculus"
mandatory=false
versionRange="*" # Accept any version of Oculus
ordering="BEFORE" # Ensures that this mod loads before Oculus if both are present
side="BOTH"

[[dependencies.${mod_id}]]
modId="iris"
mandatory=false
versionRange="*" # Accept any version of Iris
ordering="BEFORE" # Ensures that this mod loads before Iris if both are present
side="BOTH"
